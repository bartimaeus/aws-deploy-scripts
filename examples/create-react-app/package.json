{
  "name": "create-react-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.3.2",
    "react-dom": "^16.3.2",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "prebuild": "rm -fR build/*",
    "build": "react-scripts build",
    "deploy": "aws-s3-deploy --bucket app.example.com --account-id 12341234 --path build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "devDependencies": {
    "aws-deploy-scripts": "^1.3.5"
  }
}
